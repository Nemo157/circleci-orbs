parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- rustup:
    command: >
      component
      << parameters.command >>
      <<# parameters.toolchain >>
      --toolchain << parameters.toolchain >>
      <</ parameters.toolchain >>
