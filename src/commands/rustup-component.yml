parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- run:
    command: >
      rustup component
      << parameters.command >>
      <<# parameters.toolchain >>
        --toolchain << parameters.toolchain >>
      <</ parameters.toolchain >>
