parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- when:
    condition: << parameters.toolchain >>
    steps:
    - run:
        command: rustup run << parameters.toolchain >> << parameters.command >>
- unless:
    condition: << parameters.toolchain >>
    steps:
    - run:
        command: << parameters.command >>
