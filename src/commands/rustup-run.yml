parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- when:
    condition: << parameters.toolchain >>
    steps:
    - rustup:
        command: run << parameters.toolchain >> << parameters.command >>
- unless:
    condition: << parameters.toolchain >>
    steps:
    - rustup:
        command: << parameters.command >>
