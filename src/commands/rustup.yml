parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- run:
    command: >
      rustup
      << parameters.command >>
      <<# parameters.toolchain >>
        --toolchain << parameters.toolchain >>
      <</ parameters.toolchain >>
