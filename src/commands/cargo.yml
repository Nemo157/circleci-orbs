parameters:
  toolchain:
    type: string

  command:
    type: string

steps:
- run:
    command: >
      cargo
      <<# parameters.toolchain >>
        +<< parameters.toolchain >>
      <</ parameters.toolchain >>
      << parameters.command >>
